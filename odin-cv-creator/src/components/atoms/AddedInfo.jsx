const AddedInfo = ({firstValue, secondValue}) => {
  return (
    <div className="flex justify-between pt-3">
      <p>{firstValue}</p>
      <p>{secondValue}</p>
    </div>
  )
}

export default AddedInfo